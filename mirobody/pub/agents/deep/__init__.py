"""DeepAgent support sub-package.

Backend, prompt builder, tool loader, middleware, and helper modules
consumed by `mirobody.pub.agents.deep_agent.DeepAgent`. Mirrors how
`base/` supports `base_agent.py` and `mix/` supports `mix_agent.py`.
"""
